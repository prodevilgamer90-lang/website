<script>
 import { onMount } from 'svelte'; import { getHistory } from '$lib/store'; let list=[]; onMount(()=>list=getHistory());
</script>
<svelte:head><title>Continue Watching — AniVerse</title></svelte:head>
<div class="page"><a class="back" href="/">← AniVerse</a><h1>Continue Watching</h1><p>Pick up where you left off.</p>{#if !list.length}<div class="empty"><h2>Nothing here yet</h2><p>Start an episode and your progress will appear here.</p></div>{:else}<div class="grid">{#each list as a}<a href="/watch/{a.id}/{a.episode||1}" class="card"><div><img src={a.image} alt={a.title}/><span class="pct">{a.progress||0}%</span></div><h3>{a.title}</h3><p>Episode {a.episode||1} · {a.progress||0}% watched</p><i style={`width:${a.progress||0}%`}></i></a>{/each}</div>{/if}</div>
<style>:global(body){margin:0;background:#08090d;color:#fff;font-family:Inter,system-ui,sans-serif}.page{max-width:1200px;margin:auto;padding:45px 5vw 80px}.back{color:#9b7cff;text-decoration:none}.page>p{color:#777}.grid{display:grid;grid-template-columns:repeat(6,1fr);gap:20px;margin-top:35px}.card{color:inherit;text-decoration:none;position:relative}.card>div{position:relative}.card img{display:block;width:100%;aspect-ratio:2/3;object-fit:cover;border-radius:13px}.pct{position:absolute;right:8px;bottom:8px;background:#08090dcc;padding:5px 7px;border-radius:6px;font-size:11px}.card h3{font-size:14px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;margin:9px 0 5px}.card p{font-size:11px;color:#666;margin:0}.card i{display:block;height:3px;background:#8d6cf1;margin-top:9px;border-radius:99px}.empty{margin-top:100px;text-align:center;color:#777}.empty h2{color:white}@media(max-width:1000px){.grid{grid-template-columns:repeat(4,1fr)}}@media(max-width:600px){.grid{grid-template-columns:repeat(2,1fr)}}
</style>